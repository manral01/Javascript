const myObject={
    js:'JavaScript',
    cpp:'CPP',
    rb:'Ruby'
}

for (const key in myObject) {
   console.log(`${key} shortcut is for ${myObject[key]}`); // for object for object 
}

const programming = ["cpp",'rb',"java"]
